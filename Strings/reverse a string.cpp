class Solution {
public:
//    void reversestr(vector<char>&s, int i, int j){
//       reverse(s.begin(), s.end());

//    }

    void reverseString(vector<char>& s) {
        
        reverse(s.begin(), s.end());
        // int j = s.size() - 1;
        // int i = 0;
        // reversestr(s, i, j);
        // int l = 0;
        // int h = s.size() - 1;

        // while(l <= h){
        //     swap(s[l], s[h]);
        //     l++;
        //     h--;
        // }

        // reverse(s.begin(), s.end());
    }
};
